node_modules/
*.log

# Electron 빌드 산출물 (electron-builder)
dist/
dist2/
release/

# 게시/서명 토큰을 파일로 둘 경우(커밋 금지)
electron-builder.env

# IMAP 자격 등 로컬 비밀(커밋 금지)
.env

# Claude Code: 공유용 settings.json은 커밋, 개인용 local 설정은 제외
.claude/settings.local.json
dist*/
